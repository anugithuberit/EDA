import pandas as pd
import matplotlib.pyplot as plt

# Sample Daily Energy Consumption Dataset
data = {
    'Date': ['2024-01-01', '2024-01-02', '2024-01-03',
             '2024-01-04', '2024-01-05', '2024-01-06',
             '2024-01-07'],
    'Energy_Consumption': [120, 135, 128, 145, 150, 160, 155]
}

# Create DataFrame
df = pd.DataFrame(data)

# Convert Date column to datetime
df['Date'] = pd.to_datetime(df['Date'])

# Set Date as index
df.set_index('Date', inplace=True)

# Plot Time Series Graph
plt.figure(figsize=(8,5))
plt.plot(df.index, df['Energy_Consumption'], marker='o')
plt.title("Daily Energy Consumption Trend")
plt.xlabel("Date")
plt.ylabel("Energy Consumption (kWh)")
plt.grid(True)

# Display graph
plt.show()





//
import pandas as pd
import matplotlib.pyplot as plt

# Read CSV file
df = pd.read_csv("energy_consumption.csv")

# Convert Date column to datetime format
df['Date'] = pd.to_datetime(df['Date'])

# Set Date as index
df.set_index('Date', inplace=True)

# Display dataset
print(df)

# Plot Time Series Graph
plt.figure(figsize=(8,5))
plt.plot(df.index, df['Energy_Consumption'],
         marker='o', color='blue')

plt.title("Daily Energy Consumption Trend")
plt.xlabel("Date")
plt.ylabel("Energy Consumption (kWh)")
plt.grid(True)

# Display graph
plt.show()
